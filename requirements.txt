# Dashboard runtime dependencies (used by dashboard/app.py on Streamlit Cloud)
streamlit>=1.36.0
pandas>=2.2.2
plotly>=5.22.0
statsmodels>=0.14.2

# Pipeline dependencies (run locally to regenerate data/processed/)
# numpy>=1.26.4
# scikit-learn>=1.5.1
# matplotlib>=3.9.1
# seaborn>=0.13.2
# kaleido>=0.2.1
# openpyxl>=3.1.5
# kaggle>=1.6.14
# python-dotenv>=1.0.1
# quarto is a system binary, not a pip package — install separately
